{
  "name": "jot",
  "version": "0.1.0",
  "description": "Just-Output-Thoughts — a tiny CLI to quickly jot notes to SQLite",
  "type": "module",
  "bin": {
    "jot": "./bin/sqlite-launcher.mjs"
  },
  "exports": {
    ".": "./dist/cli.mjs"
  },
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "test": "set NODE_OPTIONS=--experimental-sqlite&&vitest run",
    "test:watch": "set NODE_OPTIONS=--experimental-sqlite&& vitest"
  },
  "engines": {
    "node": ">=22.5"
  },
  "license": "MIT",
  "keywords": ["cli", "notes", "sqlite", "productivity"],
  "dependencies": {
    "commander": "^12.1.0",
    "env-paths": "^3.0.0",
    "inquirer": "^12.0.0"
  },
  "devDependencies": {
    "@types/commander": "^2.12.5",
    "@types/inquirer": "^9.0.7",
    "@types/node": "^22.7.4",
    "tsup": "^8.5.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
